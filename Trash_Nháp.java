/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package pkg8quanhau;

/**
 *
 * @author Hassasin
 */
public class Trash_Nháp {
//Image_1
    
            /*
        ArrayList<Integer> a=new ArrayList();
        
        for (int i=0;i<key.length();i++){
            if (key.charAt(i)!=' '){
                String b="";
                while(key.charAt(i)!=' ') {b=b+key.charAt(i++);if (i==key.length()) break;}
                a.add(Integer.parseInt(b));
            }
        }
        for (int i=0;i<a.size();i++){
            g.drawImage(imageQ, a.get(i)*60,i*60+60, 60, 60, null);
        }*/
        //update(g);
        
        //if (!check) {update(g);}
    
            //String key=key_data.get(i);
        //String pos=pos_data.get(i);
    
        /*public void update(Graphics g){
        repaint(g);
        
        }
        public void repaint(Graphics g){// giá trị String pos format theo dạng "x y value"
        // Draw Vị Trí đang xét
        ArrayList<Integer> a=reformat(pos);
        g.drawImage(imageQ, 60, 60, 60, 60, null);
        if (a.get(2)==0){//Vị trí sai 
            g.drawImage(imageF, a.get(1)*60, a.get(0)*60, 60, 60, null);
        }else {
            g.drawImage(imageT, a.get(1)*60, a.get(0)*60, 60, 60, null);
        }
        
        // Draw Queen hiện tại
        a=reformat(key);        
        for (int i=0;i<key.length();i++){
            if (key.charAt(i)!=' '){
                String b="";
                while(key.charAt(i)!=' ') {b=b+key.charAt(i++);if (i==key.length()) break;}
                a.add(Integer.parseInt(b));
            }
        }
        for (int i=0;i<a.size();i++){
            if (a.get(i)!=0) g.drawImage(imageQ, a.get(i)*60,i*60+60, 60, 60, null);
        }
        }*/
}
